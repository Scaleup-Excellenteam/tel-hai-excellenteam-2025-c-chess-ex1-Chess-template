#pragma once

/*
*  File: Utility.h
*  Description: Header file for the Utility namespace, which contains utility functions for chess piece movement parsing.  
*/

namespace Utility {
	const char stringToUpperChar(char c);
	const int charToIntBoard(char c);
	const int stringToInt(char c);
}
